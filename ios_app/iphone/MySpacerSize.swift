import SwiftUI

struct MySpacerSize: View {

    var width = 0.0
    var height = 0.0

    var body: some View {
        ZStack {
        }
                .frame(width: width, height: height)
    }
}
