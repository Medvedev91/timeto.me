import SwiftUI
import shared

struct DaytimePickerSliderView: View {
    
    let daytimeModel: DaytimeModel
    let onChange: (DaytimeModel) -> Void

    var body: some View {

        Text("todo")
    }
}
